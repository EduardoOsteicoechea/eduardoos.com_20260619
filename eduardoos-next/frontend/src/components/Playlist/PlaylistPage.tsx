/**
 * Music page — worship library + lyrics + timed-lyric editor (admin).
 * Replaces the old create-playlist stub with PlaylistBuilder.
 */

export { default } from "../PlaylistBuilder/PlaylistBuilder";
