---
/**
 * Debate App page (product name). Persistence format remains `.edebat`;
 * JWT APIs stay under `/api/edebat`. Subscription-gated; admin always allowed.
 */
import BaseLayout from "../layouts/BaseLayout.astro";
import DebateAppPage from "../components/Edebat/DebateAppPage";
---

<BaseLayout title="Debate App — Eduardo OS">
  <DebateAppPage client:load />
</BaseLayout>
