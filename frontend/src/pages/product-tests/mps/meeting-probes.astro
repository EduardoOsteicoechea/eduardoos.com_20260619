---
/**
 * MPS meeting probes console (MPSAPS-21) — admin-only APS/ACC setup checks.
 */
import BaseLayout from "../../../layouts/BaseLayout.astro";
import MeetingProbes from "../../../components/ApsProbes/MeetingProbes";
---

<BaseLayout title="MPS meeting probes — Eduardo OS" requireAuth={true}>
  <MeetingProbes client:only="react" />
</BaseLayout>
