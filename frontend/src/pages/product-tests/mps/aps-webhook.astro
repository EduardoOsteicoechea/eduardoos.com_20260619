---
/**
 * Admin-only APS webhook live monitor (product-tests / MPS).
 */
import BaseLayout from "../../../layouts/BaseLayout.astro";
import ApsWebhookMonitor from "../../../components/ApsWebhook/ApsWebhookMonitor";
---

<BaseLayout title="APS webhook — Eduardo OS" requireAuth={true}>
  <ApsWebhookMonitor client:only="react" />
</BaseLayout>
