# MPS meeting probes — retired

**Retired 2026-09-01.** The admin console and probe APIs were removed from the website.

- Former console: `/product-tests/mps/meeting-probes`
- Former webhook monitor: `/product-tests/mps/aps-webhook`
- Former APIs: `/api/admin/aps/probes*`, `/api/aps/webhooks`, `/api/admin/aps/webhook-events*`

Autodesk APS Design Automation under `backend/aps_app/` was not changed. See `specs/031-mps-meeting-probes/spec.md` and `specs/030-aps-webhook-monitor/spec.md`.
