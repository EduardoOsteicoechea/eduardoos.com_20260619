# Milestone — Absolute 16px type + controls (2026-09-03)

Spec 063:

- Root `html` font-size **16px** × `--site-text-scale`
- Absolute `--font-*`, `--bmh`/`--bmw` (36px), `--br` — same on phone/tablet/desktop
- Inputs + `.btn` enforce `--font-base` + `--bmh`
- Spacing `--m*`/`--p*` may still shrink by breakpoint
- Pamphlet/Scrib mm/px untouched

Specs: `specs/063-absolute-type-control-sizes/spec.md`, amended `045`
