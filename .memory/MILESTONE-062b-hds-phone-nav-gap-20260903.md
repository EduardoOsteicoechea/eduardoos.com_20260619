# Milestone — HDS phone toggle + nav gap (2026-09-03)

Spec 062b:

- `useHeaderDynamicHost` retry + ready event so portals mount after Header `client:only`
- Phone tune toggle / drawer visible when HDS tools exist
- Phone nav tray flush under bar (`--header_height` = chrome; nav `top` uses bar + safe-area)
- Auth untouched

Spec: `specs/062-header-home-mobile-chrome/spec.md`
