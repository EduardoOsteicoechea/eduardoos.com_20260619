# Milestone — API docs UI-only keys + route cards (2026-09-03)

Feature 061:
- Removed `/api/apikeys` from public docs catalog (`keyPolicy` instead)
- `/api-docs` shows UI-only key note; each external route is a card

Spec: `specs/061-api-docs-ui-keys-cards/spec.md`
