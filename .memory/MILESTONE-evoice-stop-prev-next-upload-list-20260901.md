# Milestone — eVoice stop / upload list / prev-next autoplay (2026-09-01)

- Console (and Upload) shows **Stop generate** while a job runs; **Resume** when stopped
- Upload view: read-only **Uploaded documents** list (no generate/delete)
- Playlist: **Previous** + **Next** icon-only; both auto-start the target track after blob load; `ended` uses Next

Spec: `specs/044-evoice/spec.md`
