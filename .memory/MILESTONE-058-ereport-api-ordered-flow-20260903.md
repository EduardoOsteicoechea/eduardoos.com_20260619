# Milestone — eReport API ordered flow (2026-09-03)

Feature 058:
- `GET /api/v1/ereport/access` (step 1)
- `GET /api/v1/ereport/library` (step 2, owned only)
- Docs + agent prompt require access → library → get/put one at a time

Spec: `specs/058-ereport-api-ordered-flow/spec.md`
