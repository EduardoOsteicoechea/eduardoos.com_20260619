# Milestone: Header title divider + lateral pad — 2026-08-19

## Shipped (spec 013)
- Title double divider under header title (same `divider_*` language as footer Acción→Mensaje).
- Meta section `title_meta_gap` 0.6 → 2.6mm; lateral `pad_x` 3.2mm; band height 30mm.
- Desktop CSS + PDF `drawHeader` consume `header_layout`; Acción PDF size untouched.
