# Milestone — eReport (2026-08-20)

Feature 025: portable Issue Tracker in Eduardo OS.

- Subscription `ereport` ($1)
- S3 prefix `ereport/{userSafe}/…`
- Hub `/ereport/{user}` — owned + shared; create; import `.ereport`
- Editor `/ereport/{user}/{reportId}` — tema, share with registered users, tracker iframe + cloud save
- Kit source: `portable-issue-tracker/`

Spec: `specs/025-ereport/spec.md`
