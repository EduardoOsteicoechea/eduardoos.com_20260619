# Milestone: Header subtitle flush + meta top rule — 2026-08-19

## Shipped (spec 020 revision)
- Subtitle `subtitle_pad_x` / padding-left = 0 (flush with title).
- Header meta bar top gray hairline (CSS + PDF `includeTop`), matching footer meta.
