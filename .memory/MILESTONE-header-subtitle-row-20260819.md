# Milestone: Header subtitle row — 2026-08-19

## Shipped (spec 020)
- Visible editable `header.subtitle` between title divider and 2×2 meta (footer Mensaje analogue).
- Header band **35mm**; cols 1–2 / page1 body shrink (`155.9` / `119.9`).
- PDF `drawHeader` paints subtitle via `subtitle_*` in `header_layout`.
