# Milestone — Equal input/button height + visible input fill (2026-09-01)

Spec 045 amendment:
- Single-line `input` + `select` + `.btn` share exact `--bmh` height globally
- Visible `--site-input-bg` (light `#fff`, dark elevated mix) so fields do not blend into page `--bg`
- Implemented in `theme.css` / `global.css`; eReport/form-field aligned

Spec: `specs/045-global-theme-product-dashboards/spec.md`
