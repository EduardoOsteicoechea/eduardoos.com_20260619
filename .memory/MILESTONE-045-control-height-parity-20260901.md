# Milestone — Equal input/button height (2026-09-01)

Spec 045 amendment:
- Single-line `input` + `select` + `.btn` share exact `--bmh` height globally
- Visible on eReport org forms (inputs were shorter than blue buttons)
- Implemented in `global.css` + `buttons.css`; eReport/form-field aligned

Spec: `specs/045-global-theme-product-dashboards/spec.md`
